// você deixa um comentario //
// primeiro arvquivo em linguagem C //
// vamos importar a biblioteca base para executar o nosso programa //
// a biblioteca se chama stdio(ald->standard | i -> input | o -> output) 
// para importar, usaremos os comandos do #include //

#include <stdio.h> 

// abaixo, iremos escrever os comandos que serão realmente executados // 
//utilizaremos a função main(principal) para iniciar a execução //
int main(){

    printf("hello, world");
    //  o comando print serve para exibir uma mensagem em tela de console // 

    return 0;       //informar para o copilador(tradutor) gcc que o codigo acabou. o que ficar de fora não funciona // 
                    // para deixar o programa executavel colocamos no terminal gcc hello.c -o hello //
                    // para fazer ele rodar tem que colocar o nome que voce colocou "hello.c" //
}

                    